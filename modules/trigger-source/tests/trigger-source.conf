@version: 3.5

@define include-path "`include-path`:`scldir`"
@include "tests/incubator-scl.conf"

test/source-driver(
  module("trigger-source")
  subject("default")
  description("trigger() works")
  source(trigger())
);

test/source-driver(
  module("trigger-source")
  subject("trigger-freq")
  description("trigger(trigger-freq(N)) works")
  source(trigger(trigger-freq(1)))
);

test/source-driver(
  module("trigger-source")
  subject("trigger-message")
  description("trigger(trigger-message(...)) works")
  source(trigger(trigger-freq(1)
                 trigger-message("Testing!")))
);
