@version: 3.5

@define include-path "`include-path`:`scldir`"
@include "tests/incubator-scl.conf"

test/template-function(
  module("graphite")
  subject("graphite-output")
  description("$$(graphite-output) works")
  template("$(graphite-output --key PROGRAM --key PID)")
);
